package Hotel.Management.System;import javax.swing.*;public class Splash extends JFrame {    // Constructor    Splash() {        // Load the image        ImageIcon imageIcon = new ImageIcon(ClassLoader.getSystemResource("icon/splash.gif"));        JLabel label = new JLabel(imageIcon);        label.setBounds(0, 0, 858, 680);        add(label);        // Frame setup        setLayout(null);        setSize(858, 680); // Set the size of the JFrame        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // Ensure the program terminates on close        setLocationRelativeTo(null); // Center the JFrame on the screen        setVisible(true); // Make the JFrame visible        // Add a delay before closing        try {            Thread.sleep(5000); // Pause for 5 seconds            setVisible(false); // Close the splash screen after the delay        } catch (Exception e) {            e.printStackTrace(); // Print any exceptions        }    }    // Main method    public static void main(String[] args) {        new Splash(); // Create and display the JFrame    }}